import * as either from 'fp-ts/lib/Either';
import * as task from 'fp-ts/lib/Task';
import * as fetch from 'node-fetch';
import Task = task.Task;
import Either = either.Either;
export declare type fetchTaskEither = (url: string, init?: fetch.RequestInit) => Task<Either<Error, fetch.Response>>;
export declare const fetchTaskEither: fetchTaskEither;
